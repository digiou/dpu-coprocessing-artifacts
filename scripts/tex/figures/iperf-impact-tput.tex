\begin{tikzpicture}

    \begin{axis}[
        width=\columnwidth,
        height=1.65cm, % Keep the height as required
        scale only axis,
        xlabel=Time (s),
        ylabel=Tput (GiB/s),
        xlabel style={yshift=3pt},   % move X label closer to the axis
        ymin=0,
        ymax=20,
        xmin=0,
        legend style={
            legend columns=-1,
            draw=none,
            fill=none,
            font=\footnotesize,
            % anchor=north east,
            at={(0.91,1.2)}
        },
        legend to name=routing-impact-legend,
        ymajorgrids=true,
        xmajorgrids=true,
        style={xshift=0pt,yshift=0pt},
        no markers
    ]

    % Read the CSV table into a pgfplots data structure.
    \pgfplotstableread[col sep=comma]{figures/results/iperf_dpu_throughput.csv}\datatable

    % Plot each device/hardware column as a separate line.
    \addplot table [x=Time, y=DMA] {\datatable};
    \addlegendentry{DMA}

    \addplot table [x=Time, y=DFLT] {\datatable};
    \addlegendentry{DFLT}

    \addplot table [x=Time, y=LZ4] {\datatable};
    \addlegendentry{LZ4}

    % Vertical lines
    \addplot [
        color=black,
        dashed,
        thick,
    ] coordinates {(5,0) (5,20)};
    \addplot [
        color=black,
        dashed,
        thick,
    ] coordinates {(21,0) (21,20)};
    \addlegendentry{Offload Start/End}
  \end{axis}

  % === Overlay labels manually, adjust (x,y) by trial ===
%   \node[anchor=west, font=\footnotesize] at (1.8cm, 1.3cm) {Offload Start};
%   \node[anchor=west, font=\footnotesize] at (5.1cm, 1.3cm) {Offload End};
\end{tikzpicture}
    