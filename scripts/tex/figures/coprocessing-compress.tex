\begin{tikzpicture}
    \begin{axis}[
        width=\linewidth,
        height=3cm, % Keep the height as required
        ymode=log,
        ymin=10,
        ymax=10000,
        xlabel={Offload Percentage (\%)},
        ylabel={Tput (MiB/s)},
        xlabel style={yshift=3pt},   % move X label closer to the axis
        ymajorgrids=true,
        xmajorgrids=true,
        legend style={
            legend columns=-1,
            draw=none,
            fill=none,
            anchor=north east,
            at={(0.91,1.275)}
        },
        legend columns=-1,
        legend to name=coprocessing-legend,
    ]
  
      % Load table from an external CSV file.
      % Make sure the CSV file has a header row matching the column names below.
      \pgfplotstableread[col sep=comma]{figures/results/coprocessing-compress-deflate.csv}\csvdata
      \pgfplotstableread[col sep=comma]{figures/results/coprocessing-compress-deflate-arm.csv}\csvdataarm
  
      % Plot: Static
      \addplot[
        color=blue,
        mark=square*,
        line width=1pt,
        mark size=1.5pt
      ] table[x=sharing_percentage, y=static]{\csvdata};
      \addlegendentry{Static}
  
      % Plot: Reconfiguration
      \addplot[
        color=red,
        mark=triangle*,
        line width=1pt,
        mark size=1.5pt
      ] table[x=sharing_percentage, y=reconfiguration]{\csvdata};
      \addlegendentry{Reconfiguration}

      % Plot: Static ARM
      \addplot[
        color=tol_teal,
        mark=x,
        line width=.6pt
      ] table[x=sharing_percentage, y=static]{\csvdataarm};
      \addlegendentry{Static ARM}
  
    \end{axis}
\end{tikzpicture}